//go:build !embed_static

// This file provides static file serving for development mode.
// Since development uses a proxy, this handler is not actually used.

package main

import (
	"net/http"
)

// getEmbeddedStaticHandler returns a no-op handler for development
// In development mode, static files are served by the frontend dev server via proxy
func getEmbeddedStaticHandler() http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		// This should never be called in development mode since --dev flag
		// prevents static handler registration, but we need it for compilation
		http.NotFound(w, r)
	})
} 